const authConfig = {
    providers: [
        {
            domain: "https://guiding-cricket-37.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
